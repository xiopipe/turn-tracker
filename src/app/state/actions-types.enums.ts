export enum Encounter {
	LOADED = '[Encounter] Loaded success',
	LOAD = '[Encounter] Load Encounter',
	ADD = '[Encounter] add Encounter',
	ADDED = '[Encounter] added success',
	UPDATE = '[Encounter] update Encounter',
}
